# CHANGE THESE before applying — same idea as 10-mysql-secret.yaml. Prefer:
#   kubectl create secret generic backend-secret -n matterly \
#     --from-literal=jwt-secret='a-long-random-string' \
#     --from-literal=smtp-username='' \
#     --from-literal=smtp-password='' \
#     --from-literal=twilio-account-sid='' \
#     --from-literal=twilio-auth-token='' \
#     --from-literal=twilio-whatsapp-from='' \
#     --dry-run=client -o yaml > k8s/20-backend-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: matterly
type: Opaque
stringData:
  jwt-secret: changeme-generate-a-long-random-string
  smtp-username: ""
  smtp-password: ""
  twilio-account-sid: ""
  twilio-auth-token: ""
  twilio-whatsapp-from: ""
